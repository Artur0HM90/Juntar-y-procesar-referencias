# Procesamiento de Referencias desde Excel

Este proyecto contiene un script en Python que procesa un archivo Excel, filtra registros específicos y genera un nuevo archivo Excel con la información requerida.

---

## 🐍 Requisitos

### 1. Python

Se requiere **Python 3.9 o superior**.

Verifica tu versión con:

```bash
python --version

python3 --version
```

pip install pandas openpyxl

pip3 install pandas openpyxl
